<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AllParts Store</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            padding: 20px;
            background-color: black; /* Черный фон */
            color: white; /* Белый цвет текста */
            margin: 0;
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }
        h1 {
            color: red; /* Красный заголовок */
            margin-bottom: 20px;
        }
        p {
            font-size: 18px;
            margin-bottom: 40px;
        }
        .loading {
            width: 80px;
            height: 80px;
            margin: auto;
            animation: spin 2s linear infinite;
            border: 8px solid #f3f3f3;
            border-top: 8px solid red; /* Красный акцент */
            border-radius: 50%;
        }
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
    </style>
    <!-- Telegram WebApp Script -->
    <script src="https://telegram.org/js/telegram-web-app.js"></script>
    <script>
        const tg = window.Telegram.WebApp;
        tg.expand(); // Расширяет интерфейс на весь экран
    </script>
</head>
<body>
    <h1>Welcome to AllParts Store!</h1>
    <p>The store is under development. Stay tuned for updates.</p>

    <!-- Анимация загрузки -->
    <div class="loading"></div>
</body>
</html>
